#include <iostream>
#include <iomanip>
using namespace std;

struct cylinder{
    int radius,high;
    cylinder (int _radius, int _high){
       radius = _radius;
       high = _high;
    }
    double getSurfaceArea(){
        
    }
    double getVolume(){
       
    }
};

int main() {
    int radius,high;
    cin>> radius >> high;
    cylinder getSurfaceArea(radius,high) ;
    cylinder getvolume(radius,high);
    cout<<

}
